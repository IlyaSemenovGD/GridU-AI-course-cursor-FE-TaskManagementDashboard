[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
addopts = -v --tb=short
markers =
    performance: API latency / budget smoke tests (run serially in CI)
filterwarnings =
    ignore::DeprecationWarning:flasgger.*
